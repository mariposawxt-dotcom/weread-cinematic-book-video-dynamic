---
name: weread-cinematic-book-video-dynamic
description: Create a cinematic 9:16 WeRead book video from a confirmed book title and one uploaded video or audio narration file, using the default opening image and approved generated gear and title-drop sound assets. Automatically transcribe and split the single recording into intro, title, and five-part narration, then research WeRead highlights, generate a 12-book carousel, image-embedded covers, story images, bilingual subtitles, synchronized sound, and a directly watchable MP4. Use for the open-source emotional book recommendation and literary quote video workflow.
---

# 微信读书电影感视频

## 目标与用户契约

将“书名 + 正文配音”转成可直接观看的竖屏读书视频。不要要求用户准备作者、封面、书单、正文、配图、字幕或音效。

用户不知道书名时，引导其提供作者、主题、内容描述或封面印象；用微信读书搜索候选书籍，直到用户确认一个最终书名。

确认书名后，所有书名、作者、封面、文案和推荐内容都必须围绕该书生成，不得残留其他项目的固定书名。

默认交互只展示两类内容：

1. 最终正文文案，让用户准备配音。
2. 最终可直接观看的 MP4。

不要主动展示中间封面、配图、时间轴、代码、渲染日志或临时文件；用户明确要求时才展示静态帧或素材。

## 工作流

### 1. 确认书名

- 询问用户想制作哪本书。
- 如果用户只知道作者、主题或片段，搜索微信读书并给出候选，让用户确认。
- 获取正式书名、作者、题材和阅读气质。

### 2. 研究和整理正文

- 使用微信读书 Skill 获取热门划线、书评、重点内容和相似书籍。
- 选择适合连续朗读、具有共鸣感的内容。
- 默认整理五段正文，每段对应一张正文图。
- 每段只允许一个完整句子，由前后两个半句组成，中间只能使用一个逗号。
- 每段只能出现一个中文逗号“，”，不得出现第二个逗号、分号或句内句号；句末可以保留一个句号。
- 前半句和后半句各表达一个清晰意思，整体保持 12–24 个汉字（不含标点）。
- 句子要短、自然、适合一口气朗读；不得使用多个从句堆叠。
- 如果原文过长，拆成下一段，不能在同一段继续加逗号。
- 避免将语义跳跃或语气不连续的内容硬拼在一起。
- 每个段落对应一张画面和一段连续音频，不再拆成多个短句字幕。
- 先把最终正文完整展示给用户，再请求上传正文配音。

展示给用户时，使用“第一段 / 第二段”等分组标题，每组只显示一行句子；禁止换行堆叠多个句子。

合格示例：

```text
第一段
别把成功当地图，每条路都有代价。
```

不合格示例：

```text
人这一生最容易掉进去的陷阱，就是把别人的成功当成自己的地图，却忘了每条路都有无法复制的条件和代价。
```

### 3. 获取并自动拆分语音

用户只需上传一条完整的视频或音频文件（例如 `voice.mp4`、`voice.mov`、`voice.m4a` 或 `voice.mp3`）。这条素材按以下顺序包含连续人声：

```text
“今天我们分享的是” → 用户确认的书名 → 五段正文朗读
```

模型必须先从视频中提取原始音轨，再转写并自动切分为三个内部片段：

```text
intro.wav      = “今天我们分享的是”
title.wav      = 用户确认的书名
narration.wav  = 五段正文的连续朗读
```

拆分规则：

- 以转写文本中的固定短语“今天我们分享的是”和用户确认书名作为语义锚点。
- 结合语音停顿、静音区间和词级时间戳确定切点；不要按平均时长硬切。
- 先将用户上传音频转成用于分析的单声道 16 kHz 副本；渲染仍使用原始音轨的音量和音质。
- `intro.wav` 从第 0 秒开始，裁掉开头明显多余的静音，但保留“今天我们分享的是”的全部发音。
- `title.wav` 不能直接把转写段落的起点当作文件起点。先在“今天我们分享的是”之后寻找书名的第一个有效人声帧，裁掉书名前的长气口，再在书名结束后裁掉明显多余的尾部静音；不得删除“天幕红尘”等任何实际字音，也不得改变原始音量。
- 对 `title.wav` 再次转写，结果必须完整匹配用户确认的书名；只得到首字、空白或低置信度结果时，必须停止渲染并报告切点，不得生成静音标题。
- `narration.wav` 从书名实际结束后的有效语音开始，裁掉段落之间明显多余的空白，但保留正文每个字。
- 校验 `intro`、`title`、`narration` 的文字顺序、完整性和时长；检测到漏读、吃字、重复或切点置信度不足时，先向用户说明并请求确认切点，不要求其重新拆成多个文件。
- 保存每个片段的实际有效时长和有效语音起止点；不得使用旧项目遗留的固定标题时长。
- 切分后的三个文件只作为渲染内部中间产物，不向用户索要或默认展示。

最终用户不需要、也不应被要求额外上传齿轮音或水滴音。新版 Skill 使用两段已重新合成、批准纳入公开包的音效；不得把剪映原音效、来源不明的音效或旧项目音效打包进公开 Skill：

```text
gear-carousel.wav = 书籍轮播齿轮声
title-drop.wav    = 主讲书籍出现时的水滴/重点音
```

正式资源固定为：

```text
assets/gear-carousel.wav = 重新合成的齿轮轮播音，约 2 秒
assets/title-drop.wav    = 重新合成的水滴/重点音，约 1.5 秒
```

这两段文件只保留机械节奏、水滴落点和空间感等抽象声音特征，不直接使用剪映原始录音。每次渲染读取新版 Skill 的这两份资源，不从用户配音视频中提取音效，也不把音效混入用户人声。若替换资源，必须重新记录来源、生成工具、授权状态和实际时长。

验证两段资源均不含可辨识的人声，尤其不能出现旧书名“活着”；若检测到语音污染、来源不明或授权不足，先替换 Skill 资源，不能让用户重新上传音效。

不要为“今天我们分享的是”、书名或正文调用 TTS；不要把同一人声文件拼接或叠加两次。若视频没有可提取音轨或转写无法可靠定位，要求用户确认切点后再继续。

对用户配音执行以下检查：

- 转写并逐句核对正文。
- 删除多余的开头和结尾气口，但不误删正文。
- 检查漏读、吃字、重复和错序。
- 不重复叠加同一段音频。
- 不改变用户原始音量；只做必要的静音裁切和时间对齐。

### 4. 生成视觉素材

#### 固定首屏

默认直接使用随新版 Skill 提供的素材：

`assets/default-opening.png`

它对应已经确认的首屏视觉版本。

- 不重新生成首屏。
- 不替换首屏风格。
- 不添加底部小字、副标题、“摘要”、红框或平台 UI。
- 只允许添加轻微镜头运动。

#### 推荐书籍轮播

生成固定 12 本书：

- 第 1 本是用户确认的主讲书籍。
- 第 2 至第 12 本是同题材、同情绪或高度相关的书籍。
- 不重复书籍；微信读书不足时继续搜索补齐。
- 推荐书名用于轮播时可使用不超过 6 个汉字的短显示名，但不得修改正式书名。

每本书生成一张独立的 9:16 全屏封面：

- 贴近书籍内容，不照抄《活着》或其他单本书的场景。
- 真实电影剧照感、胶片感、略旧、略脏、低饱和、有故事感。
- 12 张封面使用不同主色调和不同构图。
- 书名和作者不是普通的干净排版，而是画面中的叙事元素：像旧书封、胶片片头、墙面刷字、褪色海报或被雨雾打湿的印刷字，根据场景选择一种具体质感。
- 书名使用有年代感的中文书名字体处理：可有轻微字面磨损、墨色不均、细微浮雕、边缘漏光、旧印刷偏移或颗粒渗入；必须保持笔画清晰可读，不能做成乱码或过度破损。
- 作者使用“姓名/著”，尺寸明显小于书名但不能过小；作者与书名共享同一时代材质，却可用暖灰、旧金、褪红等低饱和色形成层次。
- 所有封面的书名和作者必须使用统一的标题版式模板：相同的水平对齐方式、相同的垂直安全区、相同的书名/作者相对间距、相同的文本块宽度和基线；不得因为场景变化而移动到不同位置。
- 统一位置建议放在画面上方或上中部的固定安全区，避开主体和裁切边缘；场景变化只能通过局部明暗、材质和对比度适配，不能改变标题块坐标。
- 文字要有前后景关系：允许轻微被雾、雨痕、玻璃反光或胶片颗粒影响，但不能被人物主体遮挡；避免纯白无阴影、统一字号、完美居中、现代无衬线或模板化海报字。
- 不添加底部小字、红框、平台按钮或无关标签。

书名和作者必须在图像生成阶段直接嵌入画面，像印刷或绘制在图片上一样；禁止使用 HTML、CSS、Remotion 或 HyperFrames 在后期叠加封面文字。
图像生成提示词必须同时包含：用户确认的正式书名、作者格式“姓名/著”、具体文字材质、与场景融合的放置方式，以及“文字直接嵌入图像像素、不是后期叠加”的要求。
每张图先为文字选择一个叙事处理方案，再生成画面；不要只把同一套白色标题复制到 12 张图上。至少在 12 张图之间轮换三类以上处理：旧印刷、墙面/玻璃环境字、胶片片头/投影字。无论采用哪种处理，标题块必须锁定在同一位置。
逐张检查生成图片中的书名和作者；文字错漏、乱码或被主体遮挡时，重新生成，不能用后期文字覆盖来补救。

#### 主讲书籍画面

齿轮声结束后必须展示用户确认的书名，而不是写死任何默认书名：

`《{用户确认书名}》` 以及 `{微信读书作者}/著`

主讲书籍封面必须来自用户确认书名对应的内容。原始主讲封面画面上不再叠加“本期主讲”“摘要”、编号或其他无关内容。

#### 正文配图

生成五张正文图，一段正文对应一张图：

- 每张图贴合对应句子的场景和情绪。
- 保持真实电影感、低饱和、旧胶片和故事感。
- 五张图使用不同色调和构图。
- 在与轮播封面相同的统一标题安全区内直接嵌入书名和作者，作者使用“姓名/著”；采用与画面匹配的旧印刷/胶片/环境字质感，文字属于图片内容，不是后期字幕层。
- 不把正文字幕直接烧录进图片；字幕由视频程序渲染。

## 时间轴和镜头结构

使用 Remotion 合成，HyperFrames 保持预览结构同步。默认规格为 1080×1920、30fps、9:16。

固定顺序：

```text
默认首屏图
→ 首屏旁白“今天我们分享的是”
→ 12 本书籍全屏轮播
→ 齿轮声结束
→ 用户确认的书名和作者出现
→ 五张正文图与正文朗读
→ 结束
```

音频必须严格串行：

```text
intro.wav → gear-carousel.wav → title-drop.wav + title.wav → narration.wav
```

严格执行：

- 首屏图片从第 0 秒开始，持续到首句旁白完全结束。
- `intro.wav` 从第 0 秒开始播放；首屏停留长度等于 `intro.wav` 的有效语音长度。
- 轮播开始和结束必须匹配齿轮音效，12 本封面全屏硬切，默认不使用闪白。
- 齿轮声结束后先播放 `title-drop.wav`，并在同一时间片展示用户确认的书名封面；`title.wav` 必须从这个片段开始，不得提前或延后。
- 标题场景的持续时长必须由实际测得的 `title.wav` 时长与 `title-drop.wav` 时长共同决定：至少覆盖两者中较长者；正文只能在两段音频都结束后开始。
- Remotion 和 HyperFrames 必须从同一份时间轴清单读取 `title_start`、`title_audio_duration`、`title_drop_duration`、`content_start`；禁止写死 `0.864989`、`1.415896` 或任何旧版本标题时长。
- 齿轮声后的标题必须使用用户最终确认的书名和作者。
- 正文朗读开始后，每张正文图按对应文案和真实音频时间切换。
- 不让一句话跨到语义不属于它的画面。
- 关键短语必须和所属图片保持一致。例如“等待明天”属于倒数第二张图，就让倒数第二张图的时间轴持续到该短语音频结束，最后一张图从下一句开始。
- 缩短正文结束前的无意义空白衔接。

## 正文图片动效和清晰度

正文图不能静止。每张图必须使用克制、连续的 Ken Burns 动效：

- 缓慢推近或拉远。
- 横向或纵向轻微漂移。
- 不同图片使用不同运动方向。
- 动效贯穿整段画面停留时间。
- 不使用突然跳动、旋转、弹跳或夸张缩放。

清晰度规则：

- 优先生成或使用接近 1080×1920 的素材。
- 对低分辨率图片不要过度放大。
- 通过平移制造镜头感，额外缩放尽量控制在约 4.5% 以内。
- 全屏铺满时保持原始比例，避免全局额外放大造成发糊。

## 字幕规范

使用电影感中英双字幕：

- 中文在上、英文在下。
- 两行居中，视觉大小接近。
- 白色字体、黑色描边和轻微阴影。
- 不使用厚重字幕面板。
- 不显示“摘要”、红框或平台 UI。
- 每段字幕只显示对应的一行完整句子，不再拆成多个中文短句。
- 每段中文字幕必须与正文清单中的单句完全一致，且只包含一个中文逗号。
- 如果句子过长或逗号数量不对，回到正文整理阶段重写，不在字幕阶段修补。
- 中文和英文必须共享同一时间片段，不能错位。
- 字幕出现、消失和画面切换必须与音频一致。

## 动态书名替换

不得在实现中写死《活着》、余华或任何其他默认书名。

建立唯一内容清单，至少包含 `book_title`、`author`、`title_reveal` 和五个 `segments`；所有画面、字幕、封面提示词、输出文件名和元数据都从这份清单读取。渲染前扫描项目代码和文本清单：当用户书名不是《活着》时，出现“活着”或“余华”即视为失败；不得只替换主标题而遗漏其他场景。

用户确认书名后，动态替换：

- 齿轮声后的主讲书名和作者。
- 主讲书籍封面。
- 五张正文图的书名和作者。
- 字幕和标题中出现的书名。
- 推荐书单中的主讲书籍。
- 输出文件名和视频元数据。

首屏固定使用 `assets/default-opening.png`；轮播封面和正文配图根据本次用户确认的书名重新生成或选择。新版 Skill 与旧版 Skill 的区别仅是齿轮音和水滴音资源不同。

## 渲染与校验

使用 Remotion CLI 输出最终 MP4，并用 FFprobe 检查：

- 视频可以正常播放。
- 视频和音频总时长。
- 视频帧率和分辨率。
- 音频轨道数量，确认没有重复音频。
- 一个用户上传的视频/音频已成功提取，并从中派生出 `intro.wav`、`title.wav`、`narration.wav`；齿轮和水滴音直接来自新版 Skill 的 `assets/gear-carousel.wav`、`assets/title-drop.wav`，不能要求用户提供音效。
- 音频顺序严格为 intro → gear → title-drop+title → narration，且不改变用户人声原始音量；预制音效的来源、许可证和时长必须记录在时间轴清单中。
- `title.wav` 的有效语音必须在其播放窗口内；检查标题文件的首个有效人声帧、完整转写和实际文件时长，防止“文件存在但播放窗口全是前置静音”。
- Remotion 与 HyperFrames 的标题/正文切点必须来自同一份测量结果，不得残留旧标题时长或让正文覆盖标题尾音。
- 首屏是否从第 0 秒开始。
- 轮播是否完整覆盖齿轮音效。
- 齿轮声后的书名是否为用户确认书名。
- 五张正文图是否都有动效。
- 字幕是否和正文音频对齐。
- 关键句是否在正确图片上。
- 图片是否因过度缩放而模糊。
- 封面文字是否已经嵌入图片，而不是由后期图层添加。
- 五个正文段是否各只有一个逗号、一个句子和一行字幕。
- 项目中是否残留旧书名“活着”或旧作者“余华”。

可运行 Skill 自带的确定性自测：

```text
python scripts/validate_story_spec.py --self-test
```

自测会检查动态书名、封面文字嵌入标记、五段正文、单逗号句式、单文件语音拆分契约和旧书名残留。

## 最终用户输出

默认只交付：

1. 一段最终确认的正文文案。
2. 一个可以在对话框直接观看的 MP4。
3. 一个本地 MP4 文件链接。
4. 简短说明视频时长和音画校验结果。

不主动交付中间图片、代码、时间轴、工程日志或临时素材。
